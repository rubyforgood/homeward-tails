<% @unique_species.each do |species| %>
  <%= link_to "#{t("general.browse")} #{t("general.#{species.downcase}s")}", adoptable_pets_path(species: "#{species}"), class: "btn btn-primary btn-lg rounded-1 ms-2" %>
<% end %>