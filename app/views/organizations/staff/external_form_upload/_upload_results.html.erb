<turbo-frame id="results">
    <% if import.success? %>
        <h5 role="alert">File successsfully scanned: <%= import.count %> new form submissions were added.</h5>
    <% else %>
        <h5 role="alert">File scanned: <%= import.errors.count %> error(s) present</h5>
         <p><%= import.count %> new form submissions were added.</p>
         Review the following error(s) to ensure all form submissions are added:
         <ul>
             <% import.errors.each do |error| %>
             <li>
                 <%= "Row #{error[0]} - #{error[1].message}" %>
             </li>
             <% end %>
         </ul>
    <% end %>
</turbo-frame>
