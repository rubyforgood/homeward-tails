<%= turbo_stream.replace_all ".#{dom_id(@user.person, :role)}",
                         partial: "organizations/staff/staff/role",
                         locals: {
                           staff: @user.person,
                         } %>

<%= turbo_stream.replace_all ".user_deactivate_toggle_#{@user.person.id}",
                         partial:
                           "organizations/staff/shared/deactivate_toggle",
                         locals: {
                           person: @user.person,
                           group: @user.person.current_staff_group,
                         } %>
<%= turbo_stream.replace "flash", partial: "layouts/shared/flash_messages" %>
